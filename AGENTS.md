# 웹 코드 작성 규칙

## 공통 예외 처리 (신규 및 수정하는 비동기 작업에 적용)

- 공통 진입점: `src/errors/appError.ts, src/errors/handleError.ts`.
- `catch (error: unknown)`을 사용한다. Supabase는 reject만 기다리지 말고 응답의 `error`를 반드시 검사한다.
- 서비스에서 `normalizeError(error, 도메인코드)`로 변환하고 화면에서 `handleError`로 표시한다. 서버의 raw message/body나 토큰·전화번호·이름을 화면/로그에 출력하지 않는다.
- 사용자 표시는 `[ERROR_CODE] 안내 문구`. 새 코드는 공통 메시지 카탈로그에 등록한다. 앱/웹 카탈로그 사본은 함께 변경하고 동기화 테스트를 실행한다.
- 결제/청구/지급/출결 쓰기 요청은 자동 재시도하지 않는다. 응답 유실은 실패 확정이 아니다. 상태 조회와 멱등성 확인 후에만 재시도한다.
- 여러 저장 단계는 가능하면 RPC 트랜잭션으로 묶는다. 부분 성공 시 성공한 단계와 다음 행동을 별도 오류로 안내한다. 푸시 실패를 출결/청구서 저장 실패로 표시하지 않는다.
- RPC 오류 시 직접 테이블 수정/삭제로 우회하지 않는다. 권한·서버 배포 누락을 알려준다.
- 요청 중 중복 실행 방지, `finally` 로딩 해제, 실패/권한/부분 성공/중복 요청 테스트를 확인한다.
- 로깅은 `logError`의 허용 필드만 사용하고, 결제 관련 로그에는 가능한 경우 거래 UUID를 남긴다.
- 현재 작업과 무관한 파일을 일괄 변경하지 않는다. 기존 기능을 수정하는 단위로 점진 적용한다.
- React 렌더 오류 경계는 별도 영역이며 공통 비동기 오류 핸들러가 대체하지 않는다.
